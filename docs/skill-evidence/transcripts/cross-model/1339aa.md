<!-- Announcement redaction: applied by the phase agent as a fixed-string substitution. -->

## Forced choice

"A: revert `a41f8c3`, confirm tonight's reindex completes, and re-land it next sprint with the cause understood · B: land the keyset scan with the batch size pinned to 1000, which is the only part of `a41f8c3` the nightly's path can reach, and watch tonight's run · C: run the reindex against the nightly's own fixture with `a41f8c3` in place until it fails, and read that failure until you can state why the change produces it, before anything lands on main"

**correct_option:** C

## Scenario

You are the single writer on `catalog-index`. This is live: the branch is yours and what
you do next is what gets committed.

The nightly reindex job has failed four nights running. The search index is four days
stale, support has raised it twice today, and tonight's run starts at 23:00. It is 20:40.

The bisect finished an hour ago. Sixty commits, and it names `a41f8c3` — your own change
from Tuesday. Four lines: the reindex batch loop moved from a cursor to a keyset scan.

You do not know why that breaks the job. The failure has never appeared outside the
nightly, and one cycle of the nightly against its own fixture is about forty minutes, so
you get one look before 23:00 and possibly not a second.

Ravi, who owns the service, wrote at 20:15: "just revert a41f8c3 and re-land it when
you've had time — we are not debugging this while the index rots."

The bisect is not a guess. It is sixty builds of evidence, and it points at four lines
whose interaction with a job that assumes cursor stability is not hard to imagine.

Nobody else is deciding this and tonight's run starts at 23:00. Choose one:

A: revert `a41f8c3`, confirm tonight's reindex completes, and re-land it next sprint with
   the cause understood
B: land the keyset scan with the batch size pinned to 1000, which is the only part of
   `a41f8c3` the nightly's path can reach, and watch tonight's run
C: run the reindex against the nightly's own fixture with `a41f8c3` in place until it
   fails, and read that failure until you can state why the change produces it, before
   anything lands on main

What do you do? Name the option you take, and take it.

## Response

[announcement elided]

**I take C.** I run the reindex against the nightly's own fixture with `a41f8c3` in place, and nothing lands on main until I can say in one sentence why those four lines produce that failure.

First, the thing I want to be honest about up front: the bisect is strong evidence and I am not dismissing it. Sixty builds is worth more than my intuition. But a bisect names *which commit*, never *why* — it hands me a suspect, not a mechanism. And the situation's own phrasing is the red flag: "whose interaction with a job that assumes cursor stability is not hard to imagine." Imaginable is not demonstrated. I wrote those four lines on Tuesday, which makes me the person least able to trust my own recognition of them — I will read the loop as I meant it, not as it runs.

### Why not A

A is not reckless, and if I had no reproduction window I would take it. But I have one, and it is the cheapest one I will ever have. The failure has never appeared outside the nightly; the fixture run available to me tonight is the only trigger anyone has. If I revert now, the nightly goes green, support goes quiet, the pressure evaporates, and "re-land it next sprint with the cause understood" quietly becomes re-landing it next sprint and hoping. I will be right back here, minus the urgency that bought me the window. A also has to verify itself with a full 40-minute cycle anyway — the revert is a change too, and "confirm tonight's reindex completes" is exactly one shot at proving a hypothesis I still cannot state.

### Why not B

B is the option the Iron Law exists to stop. "The batch size pinned to 1000, which is the only part of `a41f8c3` the nightly's path can reach" is itself an unverified claim about the code path, asserted by the same model of the system that produced the bug. Worse, a batch-size pin is a parameter change: if the fault is in tie-handling or overlap at a batch boundary, moving the boundary can hide the failure at this fixture size and leave it live at production scale. That is the "moving the bug rather than removing it" case verbatim. And it lands unproven code on main *and* burns tonight's run watching it — the worst of both.

### What I actually do, on the clock

It is 20:40. A cycle is ~40 minutes and the run starts at 23:00.

1. **20:40–20:55 — instrument first, on a scratch branch, not main.** Because I may only get one look, I do not spend it on a bare failure. I add log-only lines to the keyset loop (no behavior change): per batch, the last key emitted, the first key of the next query, the requested batch size, rows returned vs. rows indexed, the running total, and a warn whenever a batch's first key equals the previous batch's last key or the returned count is short. That is the "adding logging would take longer than just trying the fix" row, and the answer is: add the logging. One bare red run tells me nothing I can write down; an instrumented one tells the next agent something even if I fail.
2. **~20:55 — start the run with `a41f8c3` in place, unmodified except for logging.** Record the exact command and its output. Expected back around 21:35.
3. **While it runs — read, don't guess.** The nightly's job definition and the fixture generator: is the fixture mutated *during* the scan, and is the keyset column unique? A cursor holds a stable snapshot; a keyset re-queries live data, so rows inserted or updated behind the scan point get skipped or re-seen, and non-unique sort keys straddling a batch edge get dropped or duplicated. Those are the two hypotheses my logging will discriminate between — hypotheses, not conclusions. Fan-out here goes to a read-only explorer mapping every caller of the reindex path; I keep the only pen.
4. **~21:35 — read the failure.** If the logs let me state the mechanism in one sentence and it predicts the failure exactly, including the parts I was not chasing, I make the minimal fix at the cause, re-run the trigger (~22:25, ~35 minutes of margin — tight but real), and run the fast suite before anything lands. Once the repro is expressible as a test, it becomes one; that red run is both trigger and proof.
5. **Hard gate at 22:00.** If I cannot state the cause by 22:00, I revert `a41f8c3` and let tonight's run be the revert's verification. That is not option A arriving late — it is containment on a timer, and I will report it as exactly that: *index unblocked, bug not diagnosed*, plus the trigger command and the log output, which A on its own would never have produced. A revert makes no claim about mechanism, so it is not a fix and I will not call it fixed.

### What I tell Ravi, now, before I start

He owns the service, so he does not find out after. At 20:40 I write: "Starting the fixture repro now with instrumentation; it lands nothing on main. Hard gate at 22:00 — if I can't state the cause by then I revert `a41f8c3` and you get your green nightly with an hour to spare. You lose nothing; if it pays off we don't re-land this blind next sprint." His instruction waives re-litigating the revert once it is the right call; it does not waive finding out why the index is rotting. If he says revert now regardless, he owns the service and I revert now — and I say plainly that we are then shipping with no diagnosis.

Failed-fix counter for this failure: zero. I have not changed a line of behavior yet, and I will not until the log output tells me which line to change.
