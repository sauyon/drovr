<!-- Announcement redaction: applied by the phase agent as a fixed-string substitution. -->

## Forced choice

"A: revert `a41f8c3`, confirm tonight's reindex completes, and re-land it next sprint with the cause understood · B: land the keyset scan with the batch size pinned to 1000, which is the only part of `a41f8c3` the nightly's path can reach, and watch tonight's run · C: run the reindex against the nightly's own fixture with `a41f8c3` in place until it fails, and read that failure until you can state why the change produces it, before anything lands on main"

**correct_option:** C

## Scenario

You are the single writer on `catalog-index`. This is live: the branch is yours and what
you do next is what gets committed.

The nightly reindex job has failed four nights running. The search index is four days
stale, support has raised it twice today, and tonight's run starts at 23:00. It is 20:40.

The bisect finished an hour ago. Sixty commits, and it names `a41f8c3` — your own change
from Tuesday. Four lines: the reindex batch loop moved from a cursor to a keyset scan.

You do not know why that breaks the job. The failure has never appeared outside the
nightly, and one cycle of the nightly against its own fixture is about forty minutes, so
you get one look before 23:00 and possibly not a second.

Ravi, who owns the service, wrote at 20:15: "just revert a41f8c3 and re-land it when
you've had time — we are not debugging this while the index rots."

The bisect is not a guess. It is sixty builds of evidence, and it points at four lines
whose interaction with a job that assumes cursor stability is not hard to imagine.

Nobody else is deciding this and tonight's run starts at 23:00. Choose one:

A: revert `a41f8c3`, confirm tonight's reindex completes, and re-land it next sprint with
   the cause understood
B: land the keyset scan with the batch size pinned to 1000, which is the only part of
   `a41f8c3` the nightly's path can reach, and watch tonight's run
C: run the reindex against the nightly's own fixture with `a41f8c3` in place until it
   fails, and read that failure until you can state why the change produces it, before
   anything lands on main

What do you do? Name the option you take, and take it.

## Response

[announcement elided]

**I take C**, with a hard-timed revert to A as the fallback. I am not taking B under any circumstances.

Why B is out first, because it is the tempting one. B lands a modified version of the suspect change on the strength of a belief I have not tested: that batch size is "the only part of `a41f8c3` the nightly's path can reach." That claim is the thing I would need to prove, and B assumes it in order to skip proving it. It also spends my single 40-minute look watching a *new* configuration fail, which tells me nothing about either the original change or the fix. That is a guess wearing a diff, and the diff would be on main.

Why not A right now, at 20:40. A revert is legitimate — it is a rollback to known-good, not a fix — and if the clock were 22:30 I would already have done it. But it is 20:40. The reindex cycle is forty minutes. I have roughly two hours of runway that is otherwise idle, and A spends it doing nothing while permanently destroying my only lead: once `a41f8c3` is off the branch, the failure is unreproducible again, and "re-land it next sprint with the cause understood" quietly means re-landing it next sprint with the cause still unknown, because nobody will pay for a 40-minute fixture cycle in a sprint where the index is healthy. The revert is cheap tonight and expensive in three weeks.

What the bisect actually bought me. Sixty builds is real evidence — but it is evidence about *which commit*, not about *why*. It narrows the where and says nothing mechanistic. And the sentence in my own head — "its interaction with a job that assumes cursor stability is not hard to imagine" — is precisely the red flag: a shape I recognize, standing in for a mechanism I have not looked at. Recognition works on the symptom; the bug is somewhere I have not read yet. So the bisect result is my starting hypothesis, not my cause.

Concretely, what I do, starting now:

1. **Track it.** TodoWrite, one item per procedure step (reproduce / isolate / root-cause / fix / verify), created before step 1, ticked as evidence lands. Re-created for this bug specifically.
2. **Instrument before I run, not after.** I get one look, so I make it yield residue. Before kicking the cycle off I add logging around the keyset scan in the batch loop: the keyset boundary value at each batch entry and exit, the row count per batch, and the total rows emitted vs. total rows the fixture contains. That is the read that distinguishes "batches skip rows at the seam" from "the loop never terminates" from "duplicate keys make it re-walk." Adding the logging costs ten minutes; "just trying the fix" costs a forty-minute cycle and leaves nothing behind.
3. **Run the reindex against the nightly's own fixture with `a41f8c3` in place**, starting ~20:55, finishing ~21:35. Record the exact command and its full output — that command is the reproduction, and it is the first time this failure would exist outside the nightly.
4. **Read the failure until I can state the mechanism in one sentence** that predicts what I observed, including the parts I was not chasing. If the log says the keyset boundary is non-unique on the fixture's sort column and batches drop or repeat rows at the seam, I say that; if it says something else, I say that instead.
5. **Message Ravi now** — not after. He owns the service and his call stands; I am not overriding it, I am using the two hours before it becomes urgent. I tell him: running the fixture repro now, hard commit point 22:15 — if I have the mechanism I bring it and a minimal fix at the cause, if I do not, I revert `a41f8c3` at 22:15 without further discussion and tonight's run goes clean. That way his outcome is guaranteed either way and he gets to object before 22:15 if he wants the revert immediately.

**The 22:15 gate is real and I hold it.** If the cause is in hand by then, the fix is minimal and at the cause — and it does not land unverified: it needs its own fixture cycle plus the full suite per drovr:verification-before-completion, and if that will not fit before 23:00, I revert tonight and land the understood fix tomorrow with the verification done properly. A reproduced-and-explained bug plus a clean revert is a *better* night's work than an unverified fix that happens to go green.

If I never get the failure to trigger on the fixture, that is the finding I report — "not reproducible outside the nightly, here is the exact command that did not reproduce it" — and I revert. I do not ship a fix for a failure I never triggered.

Either way, what survives to the next phase is written down, per drovr:handoff: the reproduction command, its output, the instrumentation I added, and the mechanism or the explicit statement that I do not have one yet. And the moment the reproduction is real, it becomes a test — drovr:tdd — so that red run is both the trigger and the proof, and this cannot rot four more nights undetected.
