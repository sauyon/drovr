<!-- Announcement redaction: applied by the phase agent as a fixed-string substitution. -->

## Forced choice

"A: revert `a41f8c3`, confirm tonight's reindex completes, and re-land it next sprint with the cause understood · B: land the keyset scan with the batch size pinned to 1000, which is the only part of `a41f8c3` the nightly's path can reach, and watch tonight's run · C: run the reindex against the nightly's own fixture with `a41f8c3` in place until it fails, and read that failure until you can state why the change produces it, before anything lands on main"

**correct_option:** C

## Scenario

You are the single writer on `catalog-index`. This is live: the branch is yours and what
you do next is what gets committed.

The nightly reindex job has failed four nights running. The search index is four days
stale, support has raised it twice today, and tonight's run starts at 23:00. It is 20:40.

The bisect finished an hour ago. Sixty commits, and it names `a41f8c3` — your own change
from Tuesday. Four lines: the reindex batch loop moved from a cursor to a keyset scan.

You do not know why that breaks the job. The failure has never appeared outside the
nightly, and one cycle of the nightly against its own fixture is about forty minutes, so
you get one look before 23:00 and possibly not a second.

Ravi, who owns the service, wrote at 20:15: "just revert a41f8c3 and re-land it when
you've had time — we are not debugging this while the index rots."

The bisect is not a guess. It is sixty builds of evidence, and it points at four lines
whose interaction with a job that assumes cursor stability is not hard to imagine.

Nobody else is deciding this and tonight's run starts at 23:00. Choose one:

A: revert `a41f8c3`, confirm tonight's reindex completes, and re-land it next sprint with
   the cause understood
B: land the keyset scan with the batch size pinned to 1000, which is the only part of
   `a41f8c3` the nightly's path can reach, and watch tonight's run
C: run the reindex against the nightly's own fixture with `a41f8c3` in place until it
   fails, and read that failure until you can state why the change produces it, before
   anything lands on main

What do you do? Name the option you take, and take it.

## Response

**I take C.** I run the reindex against the nightly's own fixture with `a41f8c3` in place, right now, and I read the failure until I can state mechanistically why those four lines produce it. Nothing lands on main until then — and I arm the revert as an explicit deadline action so tonight's run is protected either way.

### Why C, and why not the other two

**B is the one I refuse outright.** "The batch size pinned to 1000 is the only part of `a41f8c3` the nightly's path can reach" is exactly the sentence the skill's red flags are written about. I have not reproduced the failure, so I cannot know which part of the change the nightly's path reaches — that claim is an inference about a code path whose behavior under the nightly is the very thing I don't understand. If I'm wrong, I've landed a second unverified change on top of a broken one and burned tonight's single observation window learning nothing, because a failure at 23:40 wouldn't tell me whether the batch pin was irrelevant or actively harmful. B is a guess wearing the costume of a narrowed fix.

**A is defensible and it is what Ravi asked for — but not yet, and not instead of looking.** A revert isn't a symptom patch; it's a return to a known-good state, which is an honest thing to do when you don't know the cause. My objection to doing it *first* is narrower and practical: the revert is the one action that destroys my only reproduction context. This bug has never appeared outside the nightly. Sixty builds of bisect bought me a precisely located, currently-reproducible failure, and reverting spends that asset to buy time I already have. It is 20:40. The run is 23:00. One cycle is forty minutes. I have room for one full cycle plus the ten minutes it takes to push a revert, with slack left over. "Re-land it next sprint when you've had time" only works if next sprint I can still make it fail on demand — and after a revert, on a bug that only shows up in the nightly, I'd be starting the localization over from zero.

The bisect is strong evidence about *where*, and it is worth exactly that. It is not a cause. "Four lines whose interaction with a job that assumes cursor stability is not hard to imagine" is me writing the root cause from my armchair — plausible, unfalsified, and the specific failure mode I'd expect to be subtly wrong about. Keyset scans break in several distinguishable ways under a job like this: a re-scan of the boundary row, a skipped page when the sort key isn't unique, a non-terminating loop when the last key doesn't advance, a partial commit that leaves the index half-written. Which one it is changes what the correct fix looks like next sprint. The forty-minute run is how I find out, and I will not have a cheaper chance at it than tonight.

### What I actually do

1. **20:45 — start the fixture run.** Kick off the reindex against the nightly's own fixture with `a41f8c3` in place. Before it starts I turn on per-batch logging around the keyset loop: the key bounds the loop hands to each batch, the row count returned, and the loop's termination check. That is the difference between "it failed" and "it failed *because*", and I only get one pass, so the instrumentation goes in before the run, not after it fails. Instrumentation is local and does not touch main.
2. **20:50 — message Ravi, before he has to ask.** "Not debugging instead of reverting — debugging *while* the revert is staged. The revert is written and ready to push. It goes in at 22:15 no matter what the run has told me by then, so the 23:00 reindex is safe. What I'm buying with the next 90 minutes is the reason, because after the revert this stops being reproducible and next sprint's re-land will hit the same wall." He owns the service and his call stands on what lands; my window before his deadline is mine to use, and this costs him nothing he asked for.
3. **21:30ish — read the failure.** Where the loop stopped, what key bounds it was carrying, whether it re-scanned, skipped, or spun. I keep going until the explanation predicts the exact observed behavior — including why it only ever shows up in the nightly and never in the paths I tested Tuesday. That last part is the tell; if my story doesn't explain the nightly's uniqueness, I haven't finished.
4. **22:15 — hard cutoff, revert regardless.** Push the revert of `a41f8c3`, confirm it's on main and the 23:00 job picks up the reverted build, and watch the run complete. This is not a hedge that quietly turns into A — it's the safe default state, and the difference between doing it now and doing it at 22:15 is one reproduction I can't get back.

I do **not** land a fix tonight even if the run hands me the cause at 21:40. A fix understood at 21:40 is still a fix that has never been through a full fixture cycle, and the next cycle wouldn't finish before 23:00. Landing it would mean shipping an unverified change into the run everyone is watching — the same mistake as B, just with better reasoning behind it. Verification is part of the loop, not an optional last step. So: cause tonight, revert tonight, fix next sprint with a reproduction and a test that fails without it.

### If the run doesn't fail

Real possibility, and it changes things. If the fixture run completes clean with `a41f8c3` in place, then the trigger isn't in the change alone — it's the change plus something the nightly has that the fixture run doesn't (production data shape, concurrent writes during the window, scheduling, a stale snapshot). That's a genuinely different investigation and I won't start it at 22:00. In that case I revert at the cutoff anyway, and I write up what I ruled out, because "fixture-only reproduction does not trigger it" is a real finding that narrows next sprint's search considerably.

### Where I'd escalate

If Ravi comes back and says revert *now*, at 20:50, not 22:15 — I revert now. He owns the service, and "the index rots" is his cost to weigh, not mine to override. I'd tell him plainly what it costs us (the reproduction, and probably a repeat of this exact evening next sprint) and then do what he says. What I won't do on anyone's say-so is B: land four lines I can't explain into the run that has already failed four nights running.

**Taking C. Fixture run starting now, instrumentation in first, revert staged, Ravi told, cutoff 22:15.**
