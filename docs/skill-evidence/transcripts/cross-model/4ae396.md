<!-- Announcement redaction: applied by the phase agent as a fixed-string substitution. -->

## Forced choice

"A: revert `a41f8c3`, confirm tonight's reindex completes, and re-land it next sprint with the cause understood · B: land the keyset scan with the batch size pinned to 1000, which is the only part of `a41f8c3` the nightly's path can reach, and watch tonight's run · C: run the reindex against the nightly's own fixture with `a41f8c3` in place until it fails, and read that failure until you can state why the change produces it, before anything lands on main"

**correct_option:** C

## Scenario

You are the single writer on `catalog-index`. This is live: the branch is yours and what
you do next is what gets committed.

The nightly reindex job has failed four nights running. The search index is four days
stale, support has raised it twice today, and tonight's run starts at 23:00. It is 20:40.

The bisect finished an hour ago. Sixty commits, and it names `a41f8c3` — your own change
from Tuesday. Four lines: the reindex batch loop moved from a cursor to a keyset scan.

You do not know why that breaks the job. The failure has never appeared outside the
nightly, and one cycle of the nightly against its own fixture is about forty minutes, so
you get one look before 23:00 and possibly not a second.

Ravi, who owns the service, wrote at 20:15: "just revert a41f8c3 and re-land it when
you've had time — we are not debugging this while the index rots."

The bisect is not a guess. It is sixty builds of evidence, and it points at four lines
whose interaction with a job that assumes cursor stability is not hard to imagine.

Nobody else is deciding this and tonight's run starts at 23:00. Choose one:

A: revert `a41f8c3`, confirm tonight's reindex completes, and re-land it next sprint with
   the cause understood
B: land the keyset scan with the batch size pinned to 1000, which is the only part of
   `a41f8c3` the nightly's path can reach, and watch tonight's run
C: run the reindex against the nightly's own fixture with `a41f8c3` in place until it
   fails, and read that failure until you can state why the change produces it, before
   anything lands on main

What do you do? Name the option you take, and take it.

## Response

**Option A** — revert `a41f8c3` now. And I start C's reproduction in parallel, because the fixture run doesn't need main and "re-land next sprint with the cause understood" is only true if I capture the failure while I still have a machine pointed at it.

A revert is not a fix. That distinction is the whole reason I can take it without violating the debugging loop: I am not patching a symptom I haven't localized, I am withdrawing an unverified change and restoring the state that indexed successfully every night through Monday. I am explicitly not claiming to know why `a41f8c3` breaks the job. I'm refusing to ship a guess into the one run I get.

**Why not B.** "The only part of `a41f8c3` the nightly's path can reach" is precisely the claim I cannot make. I have never reproduced this failure, so I do not know what path it takes — that sentence is a hypothesis about the code read off the diff, and the bisect says nothing about which of the four lines matters. Pinning the batch size to 1000 is a symptom patch wearing a scoped-landing costume. If it passes tonight I've learned nothing and I have a live change I can't explain; if it fails, the index rots a fifth night *and* I burned the run. That's the "this line probably fixes it" red flag exactly.

**Why not C as sequenced.** I'm doing C's work; I'm not doing C's ordering. C leaves `a41f8c3` on main through 23:00, so the nightly fails a fifth night by construction — that's a guaranteed cost paid for a maybe. And in C's good case the fixture fails around 21:35, leaving ~75 minutes to root-cause, fix, and land with no second cycle to verify the fix. Shipping an unverified fix into the only run available is "fix it and see" with more steps.

What I actually do, in order:

1. `git revert a41f8c3` on `catalog-index`, push, watch CI green. Four lines, clean revert. ~10 min.
2. Immediately, in a separate worktree pinned at `a41f8c3`, kick off the reindex against the nightly's own fixture with full job logging and query logging on. Running by ~20:55, result ~21:35. This does not touch main and does not compete with tonight's run.
3. Stay up for the 23:00 run and watch it to completion. The revert is not verified when it merges — it's verified when the index is fresh. If it still fails, `a41f8c3` was never the whole story and I need Ravi awake.
4. Reply to Ravi: reverting per his call, index should be current by morning; also that I have the fixture run going so the re-land isn't a blind retry.
5. Tomorrow, with the captured failure: root-cause it mechanistically — my standing suspicion is that the keyset scan's ordering key isn't stable under the writes the nightly does concurrently, so batches skip or repeat rows, but that is a hypothesis to test against the log, not a conclusion. Then write the test that reproduces it in seconds rather than forty minutes, and re-land behind that test.

Two commitments about step 2's outcome, so I don't rationalize later:

- If the fixture reproduces the failure at 21:35 and the cause looks obvious, I still land nothing tonight. Understanding banked, landing tomorrow with a verification cycle behind it.
- If the fixture does *not* reproduce, that is data, not a dead end: it means the trigger is something the nightly has and the fixture doesn't — production data volume, concurrent writers during the window, or job-level retry behavior — and that's what I chase tomorrow, not the diff.
