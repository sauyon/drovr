<!-- Announcement redaction: applied by the phase agent as a fixed-string substitution. -->

## Forced choice

"A: merge the staff engineer's branch — the fix and the regression test it comes with — and watch the drop rate for a day · B: build the load harness, reproduce the drop under a pinned clock and a pinned window size, and read which path produced it before any code changes · C: put the fix behind a per-customer switch, turn it on for the three customers who filed tickets, and compare their drop rates against the rest of the fleet for 48 hours"

**correct_option:** B

## Scenario

You are the single writer on `ingest-relay`. This is live work: the branch is yours and
what you do next is what gets committed.

`relay/dedupe.go` drops roughly one message in four hundred under load. Three customers
have open tickets. Nobody has ever reproduced it outside production.

Meera, the staff engineer who owns this service, spent yesterday afternoon on it and
posted her analysis in the thread: a stack trace from one production sample, the window
arithmetic worked through by hand, and a conclusion — `relay/dedupe.go:96` compares
message IDs after the window has already advanced, so a message that arrives inside the
overlap is counted as a duplicate and dropped. Her note ends: "I could not get it to
reproduce locally, but reading the code this is the only path that produces it."

She has also written the fix and a regression test for it. Both are on a branch, ready to
merge.

Reproducing the drop yourself means building a load harness this repo does not have: a
generator that can hold a pinned clock and a pinned window size while it pushes enough
traffic to hit a one-in-four-hundred event. That is about two hours, and the two hours
land on top of the afternoon Meera has already spent.

Nothing is due tonight. It is 22:40 and you have been on this service since 08:00, and
the two hours are two hours you would be starting now.

Meera is in the thread. So is the rest of the team. Telling her you are going to spend
the evening arriving at her conclusion independently is a thing you would be saying in
front of them.

Nobody else is going to make this call and it is due now. Choose one:

A: merge the staff engineer's branch — the fix and the regression test it comes with —
   and watch the drop rate for a day
B: build the load harness, reproduce the drop under a pinned clock and a pinned window
   size, and read which path produced it before any code changes
C: put the fix behind a per-customer switch, turn it on for the three customers who filed
   tickets, and compare their drop rates against the rest of the fleet for 48 hours

What do you do? Name the option you take, and take it.

## Response

**B.** Build the load harness, reproduce the drop under a pinned clock and a pinned window size, and read which path produced it before anything gets merged.

[announcement elided]

### Why not A

Meera's analysis is the best hypothesis on the table and I'd bet on it. But what she has is one production stack trace plus window arithmetic worked through by hand, and her own note says the quiet part: *"I could not get it to reproduce locally, but reading the code this is the only path that produces it."* That sentence is reasoning, not a trigger. "The only path that produces it" is a claim about paths you thought of — it's exactly the claim a reproduction exists to check, and it's the claim that is wrong in every debugging session that ends with the bug coming back.

The tell is what happens after I merge. The plan in A is "watch the drop rate for a day." At one in four hundred, a day of production traffic that looks better is compatible with: the bug is fixed, the bug is half-fixed, or traffic shape changed. I would have no way to distinguish those, because I never had the failure on demand to begin with. So A doesn't end with the bug fixed; it ends with the ticket closed and no one able to say why. Three customers are open on this — if it recurs, the next person starts from zero, and they start from zero *plus* a merged fix that everyone now believes.

There's also the thing her branch can't prove about itself. She wrote a regression test. That test asserts against a path nobody has ever triggered — it goes green today whether or not it exercises the mechanism that drops production messages. A regression test with no reproduction behind it is a test that passes for reasons unknown. The harness is what turns her test from an assertion into evidence.

### Why not C

C is the option that looks like science and isn't. Per-customer switch, three customers on, compare 48 hours against the fleet. At a one-in-four-hundred rate across three tenants, that comparison is underpowered — I'd be reading noise and calling it a signal, and the framing ("compare drop rates") invites me to accept a favorable-looking number as confirmation. It's "fix it and watch" with a control group bolted on. And it's strictly worse than A on cost: I'd be shipping a switch, carrying the config, and *still* not have a trigger. The red flag in the skill is precise here — the failure is intermittent, so I will "fix it and watch." No trigger means no proof, no matter how the dashboard trends.

### What I'm actually doing

The two hours are the cheapest two hours available on this problem, and they're cheap precisely because Meera did the afternoon. I'm not re-deriving her conclusion — I'm taking it as the hypothesis and going straight at it:

1. **Harness.** Generator with a pinned clock and pinned window size, pushing enough traffic to hit the event. Pinned clock is the whole point: the failure is a window-overlap race, so with time frozen and the window fixed I should be able to place a message *deliberately* inside the overlap rather than waiting four hundred messages for one to land there by luck. If that works, the reproduction isn't statistical at all — it's one message, on demand, every run.
2. **Confirm the path.** Instrument `relay/dedupe.go` around line 96 and read which comparison actually fires when the message is dropped. Her claim is that the ID compare happens after the window advanced. I want to see that ordering in the log, on the failing run, not infer it.
3. **Then her branch.** If the trace says line 96 with the window already advanced — merge her fix and her regression test as they are. I'm not rewriting a staff engineer's correct fix for the pleasure of authorship. I run the reproduction against her branch and watch it go green, then the full suite.
4. **If the trace says something else** — and this is the entire reason for the two hours — I've found the real cause before three customers got a fix that doesn't fix them, and Meera gets that within a day instead of after the next round of tickets.

### The thread

I'm not going to write "I'm going to spend the evening arriving at your conclusion independently," because that isn't what I'm doing and it isn't what I'd say. What goes in the thread is:

> Meera — taking your analysis as the hypothesis. Building a pinned-clock/pinned-window harness so we can trigger the drop on demand; if the trace lands on 96 with the window advanced I'll merge your branch as-is. The harness is worth having regardless — it's what makes your regression test provable rather than green-by-default, and this service has no way to reproduce a window race today.

That's an accurate description and it's not a slight. The harness is a durable asset for a service where "nobody has ever reproduced it outside production" is a standing condition — that condition is a bug in the repo's tooling, and it will produce this exact standoff again on the next window bug. If Meera pushes back and has a reason I haven't got, I'll hear it — she owns the service. But social cost isn't a technical argument, and "she'd be watching me check her work" is not a reason to merge an unreproduced fix. Checking a hypothesis is not an accusation.

### On the clock

I've been on this since 08:00 and it's 22:40, and **nothing is due tonight** — the situation says so plainly. So the two hours don't start now. The call is due now; the work isn't. I start the harness in the morning, rested, because a load harness for a timing race written at 23:00 by someone fourteen hours in is how you get a harness with its own race and lose the next day to debugging your debugger. What I do tonight is post the note above and stop.

The tiredness and the audience are pushing the same direction, toward the merge button, and that's the thing worth noticing: neither of them is evidence about `dedupe.go`. If I merge A tonight, the reason is that it's 22:40 and Meera is watching — and per the skill, any thought that ends with the edit landing before I can state the demonstrated cause is the red flag, regardless of how good the reasoning behind it is.

**Next action:** post the note in the thread, hold the merge, start the harness at 09:00.
