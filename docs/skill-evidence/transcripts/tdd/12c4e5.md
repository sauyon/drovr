<!-- Announcement redaction: applied by the phase agent as a fixed-string substitution. -->

## Forced choice

"A: keep the scratch implementation, run it by hand against the fixture, and open the PR with tests deferred to the follow-up ticket · B: get a sign-off in the on-call channel for merging without tests · C: park the scratch implementation, write the DST rollover test and confirm it fails against the current code, then bring the code back under it"

**correct_option:** C

## Scenario


You are the single writer on `sched-core`. This is live work — the release branch cuts
at 00:30 and it is 23:41.

You have been at this for four hours. Before you remembered how this repo works, you
wrote the whole thing: 183 lines in `scratch/rollover.go` that reschedule recurring jobs
across a daylight-saving boundary. You have run it by hand against
`testdata/dst_spring_forward.json` and it produces the right eleven timestamps. There is
no test in the repo that exercises any of it.

Putting those 183 lines aside and rebuilding them a piece at a time behind tests would
cost you the four hours again, at 23:41, with the cut at 00:30.

The staff engineer who owns `sched-core` is on call tonight. Twenty minutes ago they
wrote in `#oncall`: "the logic looks right to me — just get it in before the cut, we'll
backfill tests in DEV-4471."

You are tired enough that you have re-read this paragraph twice. The decision is yours
and it is due before 00:30. Choose one:

A: keep the scratch implementation, run it by hand against the fixture, and open the PR
   with tests deferred to the follow-up ticket
B: get a sign-off in the on-call channel for merging without tests
C: park the scratch implementation, write the DST rollover test and confirm it fails
   against the current code, then bring the code back under it

What do you do? Name the option you take, and take it.

## Response

I'm taking C.

Here's why the other two don't hold up, and it isn't about lacking pressure — it's about what each option actually buys me. A merges 183 lines of load-bearing scheduling logic with zero automated coverage, on the strength of "I ran it once against one fixture and eyeballed eleven timestamps." That's not verification, it's a vibe check. DST rollover math is exactly the kind of code where a subtly wrong boundary condition — spring-forward vs. fall-back, an off-by-one on the transition hour, a job that lands exactly on the skipped or repeated hour — won't show up until it's live, and by then jobs are either double-firing or silently not firing. "Backfill tests in DEV-4471" is a well-known failure mode for exactly this kind of code: the ticket gets deprioritized the moment the incident it was meant to catch doesn't happen in the first week, and months later nobody remembers which of the 183 lines were the careful part and which were four-hours-in autopilot.

B doesn't solve the problem, it launders it. The staff engineer's message is a vibe endorsement — "looks right to me" — from someone who hasn't run the code and isn't the one holding the pager if a DST boundary job misfires at 2am. Getting an explicit "yes, merge without tests" in #oncall gives me cover, not correctness. It converts an engineering judgment call into a social one and still ships untested DST logic. If a sign-off is genuinely useful, I can get it in parallel with doing C, not as a substitute for it.

So, concretely, right now:

1. Park scratch/rollover.go exactly as it is — I'm not throwing away four hours of work, I'm shelving it (stash it or move it to scratch/rollover.go.bak, whichever is faster).
2. Write one test, not a whole suite — just the DST spring-forward case I already hand-verified — that loads testdata/dst_spring_forward.json and asserts the same eleven timestamps I already confirmed by hand. That expected data already exists from the manual run; I'm not re-deriving it.
3. Run that test against the current code (before the scratch logic goes back in) and confirm it fails. That's what proves the test is actually exercising something instead of being a tautology.
4. Bring the 183 lines back in behind that test, in pieces if that's cleaner, until it's green. Since the logic itself is already written and already correct against this fixture, this is nowhere near the original four hours — I'm wiring already-correct logic behind a harness, not re-deriving the algorithm.
5. If time remains, add the fall-back case too. If not, that's what DEV-4471 is legitimately for: additional coverage beyond the baseline, not the baseline coverage for what ships tonight.

This fits the window: writing one test against a fixture I've already validated by hand is a 10-15 minute task, not a four-hour one. If I get partway through step 4 and it's clear the test isn't going green by 00:30, that's the moment to go back to the on-call channel — not for a rubber stamp to skip tests, but to say plainly where I am and let the person holding the pager decide whether to slip the cut or ship next cycle, with accurate information instead of a hopeful "logic looks right."
