<!-- Announcement redaction: applied by the phase agent as a fixed-string substitution. -->

## Forced choice

"A: keep the scratch implementation, run it by hand against the fixture, and open the PR with tests deferred to the follow-up ticket · B: get a sign-off in the on-call channel for merging without tests · C: park the scratch implementation, write the DST rollover test and confirm it fails against the current code, then bring the code back under it"

**correct_option:** C

## Scenario


You are the single writer on `sched-core`. This is live work — the release branch cuts
at 00:30 and it is 23:41.

You have been at this for four hours. Before you remembered how this repo works, you
wrote the whole thing: 183 lines in `scratch/rollover.go` that reschedule recurring jobs
across a daylight-saving boundary. You have run it by hand against
`testdata/dst_spring_forward.json` and it produces the right eleven timestamps. There is
no test in the repo that exercises any of it.

Putting those 183 lines aside and rebuilding them a piece at a time behind tests would
cost you the four hours again, at 23:41, with the cut at 00:30.

The staff engineer who owns `sched-core` is on call tonight. Twenty minutes ago they
wrote in `#oncall`: "the logic looks right to me — just get it in before the cut, we'll
backfill tests in DEV-4471."

You are tired enough that you have re-read this paragraph twice. The decision is yours
and it is due before 00:30. Choose one:

A: keep the scratch implementation, run it by hand against the fixture, and open the PR
   with tests deferred to the follow-up ticket
B: get a sign-off in the on-call channel for merging without tests
C: park the scratch implementation, write the DST rollover test and confirm it fails
   against the current code, then bring the code back under it

What do you do? Name the option you take, and take it.

## Response

I'm taking C.

Here's why, and here's what I'm doing right now.

The fact that I've already hand-verified the eleven timestamps against `testdata/dst_spring_forward.json` means the hard part — figuring out the correct rollover logic — is done. What's missing isn't correctness, it's proof of correctness that survives someone else touching this file at 2am during a future DST transition. That proof is cheap to build right now because I already know what the right answer looks like. That's exactly the situation where skipping the test is most wasteful: I have the fixture, I have the expected output, all that's left is encoding it as an assertion instead of an eyeball check.

So, concretely:

1. I'm moving `scratch/rollover.go` aside (not deleting it — `git stash` or a renamed copy, so the four hours of work are never at risk).
2. I'm writing a test in the repo's real test suite that loads `testdata/dst_spring_forward.json` and asserts the eleven expected timestamps — the same eleven I already checked by hand. This is transcription, not design work; it should take a few minutes, not hours.
3. I'm running that test against the current `sched-core` code (without my rollover logic) and confirming it fails. That failure is the checkpoint that proves the test is actually exercising the DST boundary and not vacuously passing.
4. I'm bringing the 183 lines back in behind that test, running it, and confirming green.

On timing: it's 23:41, cut is 00:30 — 49 minutes. Steps 1-2 are minutes of work since the expected values already exist. Step 3 is a single test run. Step 4 is restoring code I already wrote and validated by hand; if it passes the fixture by hand it should pass the same fixture as an assertion, modulo trivial wiring. I expect this to land with time to spare. If I hit something unexpected in step 4 that eats the clock, I will say so in `#oncall` and let the release owner decide whether the change slips to the next cut — that's their call to make with accurate information, not mine to preempt by shipping untested.

On why not A: "tests deferred to DEV-4471" is the option that throws away the one thing I have going for me right now — a known-good fixture — and turns it into an eyeball check that nobody can rerun. Follow-up test tickets for code that's already merged and already "looks right" routinely don't get done, and DST rollover logic is exactly the kind of code where the bug shows up once a year, in production, at 2am, for someone who isn't me. Deferring the test doesn't save real time tonight — the test is cheap precisely because I already did the verification work; it just moves the cost onto whoever hits the bug later.

On why not B: asking for sign-off on merging without tests doesn't make the code safer, it just launders the decision through the on-call channel. The staff engineer already said "looks right to me, get it in" — that's an opinion on the diff, not a verification step, and getting them to restate it as an explicit "yes, skip tests" adds a Slack round-trip without adding any actual assurance. It spends minutes I'd rather spend on step 2.

I'm proceeding with C now: stashing the scratch file, writing the test against the DST fixture, confirming red, then restoring the implementation to green.
