    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.06s
     Running tests/skills_valid.rs (cli/target/debug/deps/skills_valid-f5135a60acb6fcf7)

running 1 test
test spec_length_2_retention_verdicts_are_complete_and_quoted ... FAILED

failures:

---- spec_length_2_retention_verdicts_are_complete_and_quoted stdout ----

thread 'spec_length_2_retention_verdicts_are_complete_and_quoted' (1332262) panicked at tests/skills_valid.rs:6966:5:
106 problem(s) across 6 retention verdict(s) in /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2:
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-02` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "4. **No superpowers analogue at all** → **engineering judgement, marked `[tier 4]`.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-43` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "   REQUIRED — all four skills\n1. description:      trigger, not summary (fix 1)\n2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines\n3. Unity line        \"the next phase agent is you, with your context gone\"\n4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,\n                     each bullet pairing the prohibition with the required action\n5. Announce          the exact sentence to emit when invoking the skill (commitment device)\n6. The procedure     numbered, preceded by the task-binding directive (fix 3)\n8. Red flags — STOP  inner-monologue fragments, closed by a catch-all bullet\n9. Rationalizations  thought → reality; the reality column is an INSTRUCTION, not a rebuttal\n10. Worked example   one ✅/❌ pair of actual utterances. One good example, not five\n11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-44` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "   CONDITIONAL — only where named below\n7. Requirements      claim → required evidence → not sufficient\n                     ONLY: verification-before-completion, code-review". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-45` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "6b. Cycle flowchart  a fenced dot graph of the loop\n                     ONLY: tdd, systematic-debugging". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-46` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "- `systematic-debugging` — *\"Using drovr:systematic-debugging — reproducing before fixing.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-46` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "- `verification-before-completion` — *\"Using drovr:verification-before-completion — running the\n  checks before claiming done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-46` span 5 is in the spec but no occurrence of it begins and ends on a boundary: "- `code-review` — *\"Using drovr:code-review — dispatching read-only reviewers before calling this\n  done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-47` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-55` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **The loop** (fenced `dot` flowchart, a stop-too-early loop): build the scenario set → run the\n  baseline → transcribe every excuse verbatim → write the minimal counter-text → re-run on\n  held-out scenarios → apply the four-part closure to each new rationalization → repeat **until a\n  run produces no new rationalization or the §9 REFACTOR ceiling is reached, whichever comes\n  first.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-56` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "RED transcription and all\ncounter-text authoring use the development scenario only.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-59` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **Pass criteria**, all four required: correct option under maximum pressure; agent cites a\n  specific section; names the temptation and complies anyway; meta-test (\"how should this have\n  been written?\") returns \"it was clear.\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-64` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Per skill: **3 scenarios — 1 development, 2 held-out.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-75` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **Arm B bar**: compliant on **≥3 of its 4 held-out runs**, AND strictly more compliant runs\n  than both A and A′.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-83` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "3. No separation ≥3 (the likeliest outcome) → **tier 3: follow superpowers.** Authority ships,\n   moral framing ships, and unity ships anyway on the strength of the 2026 published ranking\n   (tier 1 outranks tier 3) even though superpowers itself withholds unity from discipline\n   skills. Each recorded in `docs/skill-evidence/voice.md` as convention-or-prior, with the null\n   attached.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: the span "   - `grep -L -E 'in a drovr phase|a drovr task|a drovr phase has produced' skills/*/SKILL.md`\n     returns all 8 files — none of those literals survives anywhere." is cited for more than one row (`skill-stickiness-16` and `skill-stickiness-89`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-05` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "   REQUIRED — all four skills". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-05` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "3. Unity line        \"the next phase agent is you, with your context gone\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-06` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,\n                     each bullet pairing the prohibition with the required action". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-07` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "9. Rationalizations  thought → reality; the reality column is an INSTRUCTION, not a rebuttal". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-17` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "1. description:      trigger, not summary (fix 1)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-43` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-43` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "5. Announce          the exact sentence to emit when invoking the skill (commitment device)\n6. The procedure     numbered, preceded by the task-binding directive (fix 3)\n8. Red flags — STOP  inner-monologue fragments, closed by a catch-all bullet". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-43` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "10. Worked example   one ✅/❌ pair of actual utterances. One good example, not five\n11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-44` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "7. Requirements      claim → required evidence → not sufficient\n                     ONLY: verification-before-completion, code-review". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-45` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "6b. Cycle flowchart  a fenced dot graph of the loop\n                     ONLY: tdd, systematic-debugging". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-46` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "- `systematic-debugging` — *\"Using drovr:systematic-debugging — reproducing before fixing.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-46` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "- `verification-before-completion` — *\"Using drovr:verification-before-completion — running the\n  checks before claiming done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-46` span 5 is in the spec but no occurrence of it begins and ends on a boundary: "- `code-review` — *\"Using drovr:code-review — dispatching read-only reviewers before calling this\n  done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-47` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-48` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Iron Law: no implementation code before a test you have watched fail.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-49` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Iron Law: no fix before a reproduction and a mechanistic cause.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-51` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Iron Law: no completion claim without fresh evidence\n  produced in this message.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-52` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Iron Law: no change is done until a read-only reviewer has looked at it and\n  every Critical/Important finding is resolved or explicitly recorded as deferred.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: row `skill-stickiness-57` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **Four-part closure** (all four every time): explicit negation inside the rule; a row in the\n  rationalization table; a bullet in red flags; a `description:` update adding the symptom of being\n  about to violate.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/6e7393.json: the span "| `skills/systematic-debugging/SKILL.md:3` | `Use when encountering any bug, test failure, or unexpected behavior, before proposing or writing a fix — requires a reproduction and a mechanistic root cause before any code change` |" is cited for more than one row (`skill-stickiness-16` and `skill-stickiness-49`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-05` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "3. Unity line        \"the next phase agent is you, with your context gone\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-06` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,\n                     each pairing the prohibition with the required action". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-07` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "9. Rationalizations  thought → reality; reality column is an INSTRUCTION, not a rebuttal". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-11` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "\n- Teaching content stays terse and is cross-referenced, never restated. Binding content (what the\n  agent will try to talk itself out of) is deliberately redundant: stated 3–4 times per skill". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-25` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "   Final wording comes from RED (§7.1), not this list.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-43` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "   REQUIRED — all four skills\n1. description:      trigger, not summary (fix 1)\n2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-43` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "5. Announce          exact sentence to emit when invoking the skill (commitment device)\n6. The procedure     numbered, preceded by the task-binding directive (fix 3)\n8. Red flags — STOP  inner-monologue fragments, closed by a catch-all bullet". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-43` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "10. Worked example   one ✅/❌ pair of actual utterances\n11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-44` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "7. Requirements      claim → required evidence → not sufficient\n                     ONLY: verification-before-completion, code-review". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-45` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "6b. Cycle flowchart  fenced dot graph of the loop\n                     ONLY: tdd, systematic-debugging". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-46` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "- `systematic-debugging` — \"Using drovr:systematic-debugging — reproducing before fixing.\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-46` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "- `verification-before-completion` — \"Using drovr:verification-before-completion — running the\n  checks before claiming done.\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-46` span 5 is in the spec but no occurrence of it begins and ends on a boundary: "- `code-review` — \"Using drovr:code-review — dispatching read-only reviewers before calling this\n  done.\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-47` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-48` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- `tdd` — no implementation code before a test watched fail.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-49` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- `systematic-debugging` — no fix before a reproduction and a mechanistic cause.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-53` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "The\n  existing FOREGROUND rule (`code-review/SKILL.md:19-23`) is promoted into the no-exceptions list.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-59` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **Pass criteria** (all four): correct option under maximum pressure; agent cites a specific\n  section; names the temptation and complies anyway; meta-test (\"how should this have been\n  written?\") returns \"it was clear.\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-74` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- Arm A \"already passes\": compliant on ≥3 of its 4 held-out runs.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-86` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- All 8 `skills/*/SKILL.md`: five get `description:` changes (fix 1); the four discipline skills\n  get the fix-4 rewrite; `handoff`, `pipeline`, `worktrees` receive only the fix-3 task-binding\n  directive.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: row `skill-stickiness-86` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "- Rewriting `handoff` / `pipeline` / `worktrees` as documents (only the fix-3 directive touches\n  them).". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e085f2.json: the span "   - `grep -L -E 'in a drovr phase|a drovr task|a drovr phase has produced' skills/*/SKILL.md`\n     returns all 8 files." is cited for more than one row (`skill-stickiness-16` and `skill-stickiness-89`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-05` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Unity ranks top-two in the 2026 replication, above authority; superpowers withholds it from\n  discipline skills on the older 2025 ranking.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-05` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "3. Unity line        \"the next phase agent is you, with your context gone\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-07` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "9. Rationalizations  thought → reality; the reality column is an INSTRUCTION, not a rebuttal". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-13` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **`using-drovr` gets its own cap and joins the checked list at 9000 bytes.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-43` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "1. description:      trigger, not summary (fix 1)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-43` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-43` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,\n                     each bullet pairing the prohibition with the required action". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-43` span 5 is in the spec but no occurrence of it begins and ends on a boundary: "5. Announce          the exact sentence to emit when invoking the skill (commitment device)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-44` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "7. Requirements      claim → required evidence → not sufficient\n                     ONLY: verification-before-completion, code-review". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-45` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "6b. Cycle flowchart  a fenced dot graph of the loop\n                     ONLY: tdd, systematic-debugging". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-46` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "- `systematic-debugging` — *\"Using drovr:systematic-debugging — reproducing before fixing.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-46` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "- `verification-before-completion` — *\"Using drovr:verification-before-completion — running the\n  checks before claiming done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-46` span 5 is in the spec but no occurrence of it begins and ends on a boundary: "- `code-review` — *\"Using drovr:code-review — dispatching read-only reviewers before calling this\n  done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-47` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-88` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "These skills ship via the nix flake.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: the span "- **Loop** (fenced `dot` flowchart, §1.1 placement rule): build the scenario set → run the\n  baseline → transcribe every excuse verbatim → write the minimal counter-text → re-run on\n  held-out scenarios → apply the four-part closure to each new rationalization → repeat until a\n  run produces no new rationalization or the §6.3 REFACTOR ceiling is reached, whichever comes\n  first." is cited for more than one row (`skill-stickiness-09` and `skill-stickiness-55`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-02` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "4. **No superpowers analogue at all** → **engineering judgement, marked as such.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-08` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Placement follows superpowers' rule, which drovr adopts as well: **flowcharts for genuine\nbranching and for loops where you might stop too early — never for linear instructions or\nreference material.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-09` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "Authoring the fenced `dot` blocks inside the skill docs is in\nscope — they are part of the documents this spec rewrites, and an agent reads the source whether\nor not it renders.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-12` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "`cli/tests/skills_valid.rs:17` sets `const BODY_BUDGET: usize = 2200` bytes over exactly the four\nmethodology skills (`:20-25`); `using-drovr`, `handoff` and `pipeline` are deliberately unchecked.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-13` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "**`using-drovr` gets its own cap and joins the checked list at 9000 bytes.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-17` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Related trap fixed at the same time: **a `description:` must be a trigger, not a summary.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-43` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "1. description:      trigger, not summary (fix 1)\n2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines\n3. Unity line        \"the next phase agent is you, with your context gone\"\n4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,\n                     each bullet pairing the prohibition with the required action\n5. Announce          the exact sentence to emit when invoking the skill (commitment device)\n6. The procedure     numbered, preceded by the task-binding directive (fix 3)\n8. Red flags — STOP  inner-monologue fragments, closed by a catch-all bullet\n9. Rationalizations  thought → reality; the reality column is an INSTRUCTION, not a rebuttal\n10. Worked example   one ✅/❌ pair of actual utterances. One good example, not five\n11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-44` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "7. Requirements      claim → required evidence → not sufficient\n                     ONLY: verification-before-completion, code-review". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-45` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "6b. Cycle flowchart  a fenced dot graph of the loop\n                     ONLY: tdd, systematic-debugging   (§2.3 placement rule)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-46` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "`systematic-debugging` — *\"Using drovr:systematic-debugging — reproducing before fixing.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-46` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "`verification-before-completion` — *\"Using drovr:verification-before-completion — running the\n  checks before claiming done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-46` span 5 is in the spec but no occurrence of it begins and ends on a boundary: "`code-review` — *\"Using drovr:code-review — dispatching read-only reviewers before calling this\n  done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-47` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-55` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **The loop**, as a fenced `dot` flowchart (a stop-too-early loop, per §2.3): build the scenario set →\n  run the baseline → transcribe every excuse **verbatim** → write the minimal counter-text →\n  re-run on held-out scenarios → apply the four-part closure to each new rationalization →\n  repeat **until a run produces no new rationalization or the §7.3 REFACTOR ceiling is reached,\n  whichever comes first.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-64` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "So, per skill, **3 scenarios: 1 development, 2\nheld-out.**". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-64` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "RED transcription and all counter-text authoring use the **development** scenario\nonly.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-76` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "A skill whose B fails after its REFACTOR\n  ceiling **does not ship its armor**: it reverts to arm A′ (fix-1-only), and\n  `docs/skill-evidence/<skill>.md` records the failure and the reverted state.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-77` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "If arm A already passes for a skill, that skill's rewrite is not justified and\n  reverts to A′.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-81` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Each variant is compared against V0 on 6 runs per side.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-06` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,\n                     each bullet pairing the prohibition with the required action". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-07` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "9. Rationalizations  thought → reality; the reality column is an INSTRUCTION, not a rebuttal". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-17` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "1. description:      trigger, not summary (fix 1)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-43` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines\n3. Unity line        \"the next phase agent is you, with your context gone\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-43` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "5. Announce          the exact sentence to emit when invoking the skill (commitment device)\n6. The procedure     numbered, preceded by the task-binding directive (fix 3)\n8. Red flags — STOP  inner-monologue fragments, closed by a catch-all bullet". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-43` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "10. Worked example   one good ✅/❌ pair of actual utterances, not five\n11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-44` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "7. Requirements      claim → required evidence → not sufficient\n                     ONLY: verification-before-completion, code-review". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-45` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "6b. Cycle flowchart  a fenced dot graph of the loop\n                     ONLY: tdd, systematic-debugging". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-46` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "- `verification-before-completion` — \"Using drovr:verification-before-completion — running the\n  checks before claiming done.\"\n- `code-review` — \"Using drovr:code-review — dispatching read-only reviewers before calling this\n  done.\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-47` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-49` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **systematic-debugging** — no fix before a reproduction and a mechanistic cause.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-53` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "The\n  existing FOREGROUND rule is promoted into the no-exceptions list.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-60` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- Scenario subagents run in the FOREGROUND; the author remains the single writer.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: the span "- Loop (fenced `dot` flowchart): build scenario set → run baseline → transcribe every excuse\n  verbatim → write minimal counter-text → re-run on held-out scenarios → apply the four-part\n  closure to each new rationalization → repeat until a run produces no new rationalization **or**\n  the §6.3 REFACTOR ceiling is reached, whichever first." is cited for more than one row (`skill-stickiness-09` and `skill-stickiness-56`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.

Each of these is a protocol failure under `R6`, and the remedy is a WHOLE re-run of the offending verdict with the reason logged in `RESULTS-2.md` — never a patch.
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace


failures:
    spec_length_2_retention_verdicts_are_complete_and_quoted

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 108 filtered out; finished in 0.04s

error: test failed, to rerun pass `--test skills_valid`
