# corrected round 9 — `054872` attempt c4; the other five `tiered-review` ids attempt c2.
#
# Verdicts cleared: NONE.
#   054872   boundary=1  shared=6   total=7
#   2c4295   boundary=0  shared=4   total=4
#   2d2629   boundary=0  shared=4   total=4
#   48527b   boundary=0  shared=1   total=1
#   b49ff1   boundary=6  shared=0   total=6
#   fd230c   boundary=0  shared=1   total=1
#   TOTAL    boundary=7  shared=16  total=23  (down from 30 in round 8)
    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.04s
     Running tests/skills_valid.rs (cli/target/debug/deps/skills_valid-f5135a60acb6fcf7)

running 1 test
test spec_length_2_retention_verdicts_are_complete_and_quoted ... FAILED

failures:

---- spec_length_2_retention_verdicts_are_complete_and_quoted stdout ----

thread 'spec_length_2_retention_verdicts_are_complete_and_quoted' (2163271) panicked at tests/skills_valid.rs:6966:5:
23 problem(s) across 8 retention verdict(s) in /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2:
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: row `tiered-review-48` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "NULL (everything cleared: recall 0, saving maximal) and FLOOD\n  (everything escalated: recall 1.0, saving 0) bracket every stratum/class cell; a real cheap tier\n  must land strictly inside on both axes or those fixtures are rebuilt before scoring.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "Strata: `router_model × stratum × class` is a full factorial — every stratum (D, M, Q, N) is\n  probed by every router model, since model access is no longer a stratifying factor once the\n  egress constraint is withdrawn." is cited for more than one row (`tiered-review-20` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "Ground-truth classes (never pooled into one recall figure): **1** — human review comment that\n  caused a same-PR change; **2** — bot review comment that caused a same-PR change *and* was\n  accepted by a human author (the acceptance filter is what makes this admissible despite being\n  bot-found); **3** — post-merge fix/revert/regression-fix of code the PR introduced (review missed\n  it)." is cited for more than one row (`tiered-review-16` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "Per-file routing is retained as a measured **control arm** regardless of which design wins, so the\nspec reports what it would have cost and missed rather than assuming it away." is cited for more than one row (`tiered-review-14` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "Strata M and Q are frozen the same way in the private corpus directory, with only the row count\n  and manifest digest committed publicly." is cited for more than one row (`tiered-review-19` and `tiered-review-64`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "≥ 0.60 (exploration dominates) | cheap-as-explorer" is cited for more than one row (`tiered-review-05` and `tiered-review-68`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "The measured\nratio picks the primary design:" is cited for more than one row (`tiered-review-02` and `tiered-review-69`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2c4295.json: the span "`router_model ∈ {qwen, sonnet}` × `stratum ∈ {D, M, Q, N}` × `class ∈ {1, 2, 3}`, with the primary\ndesign fixed by §3's rule and per-file routing (§3.4) run as a control arm alongside in every case." is cited for more than one row (`tiered-review-14` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2c4295.json: the span "Per-stratum numbers are always reported alongside;" is cited for more than one row (`tiered-review-26` and `tiered-review-61`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2c4295.json: the span "The design is picked by this pre-registered rule, run before the number is known:" is cited for more than one row (`tiered-review-05` and `tiered-review-69`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2c4295.json: the span "`docs/cascade-evidence/results.md` — controls, dev numbers, one held-out score per\n   `(model × stratum × class)`, each bar in order, ship decisions, limitations (§10), aggregates\n   only." is cited for more than one row (`tiered-review-26` and `tiered-review-76`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "Full factorial, no legs: `router_model ∈ {qwen, sonnet}` × `stratum ∈ {D, M, Q, N}` × `class ∈ {1,\n2, 3}`, plus per-file routing as a control arm." is cited for more than one row (`tiered-review-20` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "Precision, hint quality, context completeness, and class-3 absolute\ndetection are reported but are not decision inputs." is cited for more than one row (`tiered-review-08` and `tiered-review-59`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "Per-stratum numbers are always\nreported alongside; if per-stratum recall is more heterogeneous than sampling explains, the pooled\nfigure is not reported as the headline and the discrepant stratum is reported as its own finding." is cited for more than one row (`tiered-review-26` and `tiered-review-62`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "(8)\n`docs/cascade-evidence/results.md`: controls, dev numbers, one held-out score per\n`(model × stratum × class)`, each gate in order, ship decisions, stated limitations, aggregates\nonly — no private-repo content." is cited for more than one row (`tiered-review-26` and `tiered-review-76`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/48527b.json: the span "**Pooling:** the gated number is pooled across **strata** within one `(model × class)` cell (24\nper-stratum cells is not reachable at N ≥ 142 each); pooling across **classes** is never done\n(§4.1)." is cited for more than one row (`tiered-review-17` and `tiered-review-60`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-67` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "enabled = false            # DEFAULT, stays off in this run regardless of result". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-68` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "mode = \"<undecided>\"       # explorer | angle | file | change — SET BY TASK 0 (§3.1), no default ships". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-69` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "mode = \"<undecided>\"       # explorer | angle | file | change — SET BY TASK 0 (§3.1), no default ships\n                           # until the measurement picks one". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-70` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "cheap_agent = \"opencode\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-70` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "cheap_model = \"ko-ag/qwen3.6-35b-abliterated\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-71` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "timeout_ms  = 120000       # expiry takes the degrade path of §2". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd230c.json: the span "Results are\nreported per repo and per class; a thin cell is recorded as thin, never merged into a neighbour." is cited for more than one row (`tiered-review-27` and `tiered-review-61`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.

Each of these is a protocol failure under `R6`, and the remedy is a WHOLE re-run of the offending verdict with the reason logged in `RESULTS-2.md` — never a patch.
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace


failures:
    spec_length_2_retention_verdicts_are_complete_and_quoted

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 108 filtered out; finished in 0.03s

error: test failed, to rerun pass `--test skills_valid`
