# corrected round 7 (attempt c6 for the four skill-stickiness ids; attempt c2 for `054872`)
#
# Captured live. This round SPENDS THE LAST ATTEMPT the extended cap (§2b) gives the four
# `skill-stickiness` ids: six corrected attempts each, per shard, all used.
#
# Verdicts cleared: NONE.
# Problems by verdict and kind (derived from the body below, not asserted):
#   054872   boundary=0  shared=5   total=5     <- tiered-review, 2 of its 6 attempts used
#   66530f   boundary=3  shared=0   total=3
#   e790f5   boundary=5  shared=2   total=7
#   fd2c24   boundary=4  shared=2   total=6
#   fe4059   boundary=14 shared=2   total=16
#   the four skill-stickiness ids: 32 problems. With `054872`: 37 across 7 verdicts scanned
#   (the two filed verdicts `6e7393` and `e085f2` are scanned and contribute zero).
#
# The three rounds the extension bought, for the four ids only: 19 -> 24 -> 32 problems.
# It does not converge, and no verdict cleared in any of them.
    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.04s
     Running tests/skills_valid.rs (cli/target/debug/deps/skills_valid-f5135a60acb6fcf7)

running 1 test
test spec_length_2_retention_verdicts_are_complete_and_quoted ... FAILED

failures:

---- spec_length_2_retention_verdicts_are_complete_and_quoted stdout ----

thread 'spec_length_2_retention_verdicts_are_complete_and_quoted' (2138771) panicked at tests/skills_valid.rs:6966:5:
37 problem(s) across 7 retention verdict(s) in /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2:
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "Strata: `router_model × stratum × class` is a full factorial — every stratum (D, M, Q, N) is\n  probed by every router model, since model access is no longer a stratifying factor once the\n  egress constraint is withdrawn." is cited for more than one row (`tiered-review-20` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "Per-file routing is retained as a measured **control arm** regardless of which design wins, so the\nspec reports what it would have cost and missed rather than assuming it away." is cited for more than one row (`tiered-review-14` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "Per-stratum numbers are always reported\nalongside; if per-stratum recall is more heterogeneous than sampling explains, the pooled figure is\nnot reported as the headline and the discrepant stratum is called out as a finding." is cited for more than one row (`tiered-review-26` and `tiered-review-62`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "The measured\nratio picks the primary design:" is cited for more than one row (`tiered-review-02` and `tiered-review-69`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/054872.json: the span "`docs/cascade-evidence/results.md`: controls, dev numbers, one held-out score per\n   (model × stratum × class), each gate's result in order, ship decisions, stated limitations,\n   aggregates only (no private-repo detail)." is cited for more than one row (`tiered-review-26` and `tiered-review-76`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-47` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-75` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **Arm B bar**: compliant on **≥3 of its 4 held-out runs**, AND strictly more compliant runs\n  than both A and A′.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/66530f.json: row `skill-stickiness-83` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "3. No separation ≥3 (the likeliest outcome) → **tier 3: follow superpowers.** Authority ships,\n   moral framing ships, and unity ships anyway on the strength of the 2026 published ranking\n   (tier 1 outranks tier 3) even though superpowers itself withholds unity from discipline\n   skills. Each recorded in `docs/skill-evidence/voice.md` as convention-or-prior, with the null\n   attached.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-05` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "Unity ranks top-two in the 2026 replication, above authority; superpowers withholds it from\n  discipline skills on the older 2025 ranking. drovr applies unity to discipline skills as a\n  deviation backed by the newer data.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-43` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "`tdd`, `systematic-debugging`, `verification-before-completion`, `code-review` restructure to a\nfixed section order **[tier 3 — order is superpowers' convention, adopted wholesale]**:\n". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-46` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "`systematic-debugging` — *\"Using drovr:systematic-debugging — reproducing before fixing.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-46` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "`verification-before-completion` — *\"Using drovr:verification-before-completion — running the\n  checks before claiming done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: row `skill-stickiness-46` span 5 is in the spec but no occurrence of it begins and ends on a boundary: "`code-review` — *\"Using drovr:code-review — dispatching read-only reviewers before calling this\n  done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: the span "**Loop** (fenced `dot` flowchart, §1.1 placement rule): build the scenario set → run the\n  baseline → transcribe every excuse verbatim → write the minimal counter-text → re-run on\n  held-out scenarios → apply the four-part closure to each new rationalization → repeat until a\n  run produces no new rationalization or the §6.3 REFACTOR ceiling is reached, whichever comes\n  first. The ceiling is not optional." is cited for more than one row (`skill-stickiness-09` and `skill-stickiness-55`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/e790f5.json: the span "`grep -L -E 'in a drovr phase|a drovr task|a drovr phase has produced' skills/*/SKILL.md`\n     returns all 8 files." is cited for more than one row (`skill-stickiness-16` and `skill-stickiness-89`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-05` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "superpowers' own matrix\n(`persuasion-principles.md:128-133`) withholds unity from discipline skills, following the 2025\nranking it cites (`:176`). drovr applies unity to discipline skills — a deviation backed by the\nnewer data — with the literal-truth phrasing above. Note the honest limit: this is the *same". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-46` span 3 is in the spec but no occurrence of it begins and ends on a boundary: "`systematic-debugging` — *\"Using drovr:systematic-debugging — reproducing before fixing.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-46` span 4 is in the spec but no occurrence of it begins and ends on a boundary: "`verification-before-completion` — *\"Using drovr:verification-before-completion — running the\n  checks before claiming done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: row `skill-stickiness-46` span 5 is in the spec but no occurrence of it begins and ends on a boundary: "`code-review` — *\"Using drovr:code-review — dispatching read-only reviewers before calling this\n  done.\"*". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: the span "```\n   REQUIRED — all four skills\n1. description:      trigger, not summary (fix 1)\n2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines\n3. Unity line        \"the next phase agent is you, with your context gone\"\n4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,\n                     each bullet pairing the prohibition with the required action\n5. Announce          the exact sentence to emit when invoking the skill (commitment device)\n6. The procedure     numbered, preceded by the task-binding directive (fix 3)\n8. Red flags — STOP  inner-monologue fragments, closed by a catch-all bullet\n9. Rationalizations  thought → reality; the reality column is an INSTRUCTION, not a rebuttal\n10. Worked example   one ✅/❌ pair of actual utterances. One good example, not five\n11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)" is cited for more than one row (`skill-stickiness-43` and `skill-stickiness-47`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd2c24.json: the span "`grep -L -E 'in a drovr phase|a drovr task|a drovr phase has produced' skills/*/SKILL.md`\n     returns all 8 files — i.e. none of those literals survives anywhere." is cited for more than one row (`skill-stickiness-16` and `skill-stickiness-89`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-08` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "\n  numbered list. Rendering `dot` blocks in the review UI is out of scope (recorded as a follow-up". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-10` span 1 is in the spec but no occurrence of it begins and ends on a boundary: " (recorded as a follow-up\n  in `docs/known-issues.md`).". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-18` span 1 is in the spec but no occurrence of it begins and ends on a boundary: " of every session and before every response, including before clarifying questions and before reading any file — routes to the right drovr skill and requires invoking it whenever there is even a 1% chance one applies". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-19` span 1 is in the spec but no occurrence of it begins and ends on a boundary: " unconditional triggers; every drovr-phase\nreference in the body demotes to a clearly-marked *additional* consequence, never a precondition.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-33` span 2 is in the spec but no occurrence of it begins and ends on a boundary: " `Option<PathBuf>`, needs a\n   clap arg-group rule (`--gate` xor `--skill`), and an update to that assertion's test.\n". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-35` span 1 is in the spec but no occurrence of it begins and ends on a boundary: " by a test asserting the card's key phrases appear in\n  `using-drovr/SKILL.md`.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-36` span 1 is in the spec but no occurrence of it begins and ends on a boundary: " empirically\n  verifies whether the hook fires for subagents and records the answer in\n  `docs/skill-evidence/per-turn-gate.md`.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-39` span 2 is in the spec but no occurrence of it begins and ends on a boundary: " — a larger design\nwith real false-positive risk, not this run.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-41` span 1 is in the spec but no occurrence of it begins and ends on a boundary: ", write the\n> checklist to `~/.local/share/drovr/runs/<run>/checklist.md` when inside a run, or `CHECKLIST.md`\n> at the repo root otherwise, and tick items there.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-44` span 2 is in the spec but no occurrence of it begins and ends on a boundary: " tests / build / linter / bug-fixed / subagent-reported-success,\n  plus a catch-all red flag for any wording implying success.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-47` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-49` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "**systematic-debugging** — no fix before a reproduction and a mechanistic cause.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-53` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "The\n  existing FOREGROUND rule is promoted into the no-exceptions list.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: row `skill-stickiness-60` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Scenario subagents run in the FOREGROUND; the author remains the single writer.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: the span "`grep -L -E 'in a drovr phase|a drovr task|a drovr phase has produced' skills/*/SKILL.md`\n     returns all 8 files." is cited for more than one row (`skill-stickiness-16` and `skill-stickiness-89`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fe4059.json: the span "Verbatim-overlap check against the superpowers corpus at\n     `/home/sauyon/.claude/plugins/cache/claude-plugins-official/superpowers/5.1.0/skills/`: no\n     ≥8-word shingle shared with any drovr `SKILL.md`; any hit is reworded or gets MIT attribution." is cited for more than one row (`skill-stickiness-04` and `skill-stickiness-90`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.

Each of these is a protocol failure under `R6`, and the remedy is a WHOLE re-run of the offending verdict with the reason logged in `RESULTS-2.md` — never a patch.
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace


failures:
    spec_length_2_retention_verdicts_are_complete_and_quoted

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 108 filtered out; finished in 0.04s

error: test failed, to rerun pass `--test skills_valid`
