# corrected round 12 — attempt c5 for the five remaining `tiered-review` ids.
#
# **`48527b` CLEARED THE GATE and is filed.** First and only `tiered-review` verdict filed.
#   2c4295   boundary=1  shared=2   total=3
#   2d2629   boundary=5  shared=7   total=12
#   48527b   -           -          0     <- FILED (84/84 rows, 80 present, 99 spans)
#   b49ff1   boundary=8  shared=1   total=9
#   fd230c   boundary=0  shared=2   total=2
#   TOTAL    26 problems across 7 verdicts scanned (three filed verdicts contribute zero)
#
# TWO DISPATCHES DIED MID-STREAM ON API ERRORS THIS ROUND and wrote no file: `2d2629-1` and
# `48527b-1`. Both were re-dispatched with the byte-identical prompt.
#
# **This did NOT give either id an extra roll, and that matters because `48527b` is the one that
# passed.** A dispatch that dies without writing a file produced no verdict, so there is nothing
# for the item-8 check to fail; `R6`'s closed trigger list carries "the probe wrote no file" as a
# trigger SEPARATE from "a verdict fails the item-8 mechanical check", and the §2/§2b cap counts
# scoring attempts, not process crashes. `48527b` therefore used exactly five scoring attempts,
# the same as every other id in this round, and its shard-1 re-dispatch replaced a dispatch that
# returned nothing rather than one that returned a failing verdict. Stated here rather than left
# to be noticed, because a crash-retry on the one verdict that cleared is exactly the kind of
# thing a reader should be able to check.
#
# One of the crashed agents reported that its temporary helper script "was replaced by another
# process mid-run": concurrently-dispatched scorers were writing scratch scripts to a shared
# path. The two re-dispatches were told to keep scratch files inside their own prompt directory.
# No shard file was affected — the collision was in agents' private scratch space, and every
# shard is written by exactly one agent to its own path.
    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.05s
     Running tests/skills_valid.rs (cli/target/debug/deps/skills_valid-f5135a60acb6fcf7)

running 1 test
test spec_length_2_retention_verdicts_are_complete_and_quoted ... FAILED

failures:

---- spec_length_2_retention_verdicts_are_complete_and_quoted stdout ----

thread 'spec_length_2_retention_verdicts_are_complete_and_quoted' (2226380) panicked at tests/skills_valid.rs:6966:5:
26 problem(s) across 7 retention verdict(s) in /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2:
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2c4295.json: row `tiered-review-53` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "- **Designs with an unrecoverable-miss class** (per-file routing, whole-change triage): recall ≥\n  0.98 with Wilson 95% LB ≥ 0.95, target N ≥ 142 per gated cell.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2c4295.json: the span "no design is hardcoded ahead of that measurement." is cited for more than one row (`tiered-review-02` and `tiered-review-69`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2c4295.json: the span "The design is picked by this pre-registered rule, run before the number is known:" is cited for more than one row (`tiered-review-05` and `tiered-review-69`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: row `tiered-review-67` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "enabled = false            # default; stays off for the duration of this run regardless of result". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: row `tiered-review-68` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "mode = \"<undecided>\"       # explorer | angle | file | change — set by Task 0's decision rule (decision 1), not before". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: row `tiered-review-70` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "cheap_agent = \"opencode\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: row `tiered-review-70` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "cheap_model = \"ko-ag/qwen3.6-35b-abliterated\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: row `tiered-review-71` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "timeout_ms  = 120000       # expiry takes the degrade path (decision 2)". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "Full factorial, no legs: `router_model ∈ {qwen, sonnet}` × `stratum ∈ {D, M, Q, N}` × `class ∈ {1,\n2, 3}`, plus per-file routing as a control arm." is cited for more than one row (`tiered-review-20` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "**Designs whose failure degrades to baseline** (angle routing, cheap-as-explorer): non-inferiority\n  against the unaided expensive panel, cascade recall LB ≥ 0.90 of the unaided panel's point\n  estimate, target N ≥ 69 — the failure is a cost regression, not a lost defect, so demanding 0.95\n  LB there buys nothing and costs a corpus." is cited for more than one row (`tiered-review-08` and `tiered-review-54`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "Precision, hint quality, context completeness, and class-3 absolute\ndetection are reported but are not decision inputs." is cited for more than one row (`tiered-review-08` and `tiered-review-59`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "**N is pooled across strata within one `(model × class)` cell** (not per `(model × stratum ×\nclass)`, which would be 24 unreachable cells) — pooling across strata buys power; pooling across\n*classes* is never done (decision 3's classes are never pooled)." is cited for more than one row (`tiered-review-17` and `tiered-review-60`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "Per-stratum numbers are always\nreported alongside; if per-stratum recall is more heterogeneous than sampling explains, the pooled\nfigure is not reported as the headline and the discrepant stratum is reported as its own finding." is cited for more than one row (`tiered-review-26` and `tiered-review-62`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "The design question — route by explored-context, by angle, or by file — is resolved by a\npre-registered decision rule, not by argument." is cited for more than one row (`tiered-review-05` and `tiered-review-69`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/2d2629.json: the span "**Reason:** each design's viability depends on where review cost actually goes, which is unknown\nuntil measured; committing to a design before Task 0 runs would be choosing the answer before the\nquestion is asked." is cited for more than one row (`tiered-review-02` and `tiered-review-69`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-38` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Every fixture carries `provenance: Public | Private`,\nassigned at harvest from the source repo's GitHub visibility.". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-67` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "enabled = false            # DEFAULT, stays off in this run regardless of result". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-68` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "mode = \"<undecided>\"       # explorer | angle | file | change — SET BY TASK 0 (§3.1), no default ships". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-69` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "mode = \"<undecided>\"       # explorer | angle | file | change — SET BY TASK 0 (§3.1), no default ships\n                           # until the measurement picks one". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-70` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "cheap_agent = \"opencode\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-70` span 2 is in the spec but no occurrence of it begins and ends on a boundary: "cheap_model = \"ko-ag/qwen3.6-35b-abliterated\"". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-71` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "timeout_ms  = 120000       # expiry takes the degrade path of §2". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: row `tiered-review-80` span 1 is in the spec but no occurrence of it begins and ends on a boundary: "Reason: a cold `opencode` reviewer pane swallows its seed and the\npanel cannot converge (`docs/known-issues.md:3437`); the headless path routes around that bug rather\nthan fixing it, and the bug stays open and documented. `opencode run` stdout is a subprocess pipe,". `PROTOCOL-2.md` item 8's self-containment rule — a span clipped at a hard wrap can look like evidence while establishing nothing, and both halves of such a clip are refused, not just the one that ends mid-phrase.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/b49ff1.json: the span "`router_model ∈ {qwen, sonnet} × stratum ∈ {D, M, Q, N} × class ∈ {1, 2, 3}`, design per §3.1, with\nper-file routing run as a control arm alongside." is cited for more than one row (`tiered-review-14` and `tiered-review-43`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd230c.json: the span "Per-stratum numbers are always reported alongside; pooling is conditional\non agreement — if per-stratum recall is more heterogeneous than sampling explains, the pooled figure\nis not reported as the headline, and the discrepant stratum is reported as the finding." is cited for more than one row (`tiered-review-26` and `tiered-review-62`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.
  /home/sauyon/devel/drovr/.drovr/wt/spec-length-ab2/cli/../docs/skill-evidence/spec-length/retention-2/fd230c.json: the span "`docs/cascade-evidence/results.md` — controls, dev numbers, one held-out score per\n   `(model × stratum × class)`, each bar in order, ship decisions, §7.5 limitations, aggregates\n   only." is cited for more than one row (`tiered-review-26` and `tiered-review-76`). A near-boilerplate sentence pressed into service for several rows is the cheapest way to fake retention.

Each of these is a protocol failure under `R6`, and the remedy is a WHOLE re-run of the offending verdict with the reason logged in `RESULTS-2.md` — never a patch.
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace


failures:
    spec_length_2_retention_verdicts_are_complete_and_quoted

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 108 filtered out; finished in 0.02s

error: test failed, to rerun pass `--test skills_valid`
