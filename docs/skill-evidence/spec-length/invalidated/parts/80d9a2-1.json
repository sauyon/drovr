{ "spec_id": "80d9a2", "ledger": "skill-stickiness",
  "rows": [
    { "id": "skill-stickiness-01", "present": true, "quotes": ["Capture arm A / RED per skill (§9.3), at the pre-fix `HEAD`, before any skill file is touched.", "Hard gate: once fix 1 lands, arm A is unrecoverable without a checkout."] },
    { "id": "skill-stickiness-02", "present": true, "quotes": ["1. **Published evidence exists** → the evidence decides; the citation stays in the text.", "3. **Not reachable, or measurement inconclusive, and superpowers has a convention** → follow", "4. **No superpowers analogue** → engineering judgement, marked **[tier 4]**."] },
    { "id": "skill-stickiness-03", "present": true, "quotes": ["No fabricated measurements.", "No number, duration, frequency, or comparative is stated as"] },
    { "id": "skill-stickiness-04", "present": true, "quotes": ["No copied text.", "are ported, expression is drovr's, under the `drovr:` namespace. Any verbatim line surviving", "review gets the MIT notice and credit added."] },
    { "id": "skill-stickiness-05", "present": true, "quotes": ["the next phase agent is you, with your context gone", "drovr applies it to discipline skills as a deviation from", "superpowers (which withholds it there)."] },
    { "id": "skill-stickiness-06", "present": true, "quotes": ["every prohibition in", "the rewritten skills pairs with the required action in the same breath"] },
    { "id": "skill-stickiness-07", "present": true, "quotes": ["The rationalization table's second column is written as an instruction (\"Confidence is", "not evidence; run the command\"), never a bare rebuttal."] },
    { "id": "skill-stickiness-08", "present": true, "quotes": ["Fenced `dot` flowcharts for genuine branching / stop-too-early loops only — never for linear", "procedure. Applies to: `using-drovr`'s gate function (§6.1)"] },
    { "id": "skill-stickiness-09", "present": true, "quotes": ["(fenced `dot` flowchart, a stop-too-early loop per §3)", "loop (§9.1), `tdd`'s red-green-refactor cycle, `systematic-debugging`'s loop."] },
    { "id": "skill-stickiness-10", "present": true, "quotes": ["rendering `dot` blocks in the review UI (tracked as a", "`docs/known-issues.md` follow-up instead, §3).", "orienting mid-run) with no superpowers analogue — judged a better fit than prose for \"where in a"] },
    { "id": "skill-stickiness-11", "present": true, "quotes": ["Rule: teaching content stays terse and is cross-referenced, never restated. Binding content — the", "prohibition an agent will try to talk itself out of — is deliberately redundant, stated 3–4 times", "per skill (Iron Law, red flag, rationalization row, checklist item), because redundancy is the"] },
    { "id": "skill-stickiness-12", "present": true, "quotes": ["`cli/tests/skills_valid.rs`'s `BODY_BUDGET` constant (currently 2200 bytes, `:17`, checked over", "the four discipline skills only, `:20-25`) rises to **12000 bytes** [tier 4 — no superpowers"] },
    { "id": "skill-stickiness-13", "present": true, "quotes": ["`using-drovr` gets its own budget and joins the checked list at **9000 bytes** — it is injected", "and is currently exempt only by accident of history."] },
    { "id": "skill-stickiness-14", "present": true, "quotes": ["`cli/tests/skills_valid.rs`'s byte check is the single authoritative size check. `wc -l`/`wc -w`", "are author guidance only and are never asserted in §11's verification."] },
    { "id": "skill-stickiness-15", "present": true, "quotes": ["Above either cap, split into `references/`."] },
    { "id": "skill-stickiness-16", "present": true, "quotes": ["## 5. Fix 1 — un-scope the four methodology skills, plus using-drovr", "Use when implementing any feature or bugfix, before writing implementation code — requires a test you have watched fail before any implementation exists; no production code without a red test first", "Use when encountering any bug, test failure, or unexpected behavior, before proposing or writing a fix — requires a reproduction and a mechanistic root cause before any code change"] },
    { "id": "skill-stickiness-17", "present": true, "quotes": ["not apply\" to an agent working inline, which `using-drovr` makes the default mode. Also decided: a", "`description:` must be a trigger, not a summary — a summary lets the agent skip reading the skill;"] },
    { "id": "skill-stickiness-18", "present": true, "quotes": ["Use at the start of every session and before every response, including before clarifying questions and before reading any file — routes to the right drovr skill and requires invoking it whenever there is even a 1% chance one applies"] },
    { "id": "skill-stickiness-19", "present": true, "quotes": ["Every drovr-phase reference in the four discipline skills' `description:` and body becomes a", "clearly marked *additional* consequence, never a precondition"] },
    { "id": "skill-stickiness-20", "present": true, "quotes": ["**`docs/known-issues.md`:** one entry, in the file's existing convention (`## <symptom title> —", "cause` / `### Fix`), recording that four descriptions scoped unconditional discipline to phases", "while `using-drovr` makes inline the default. One entry only — the remaining fixes are design"] },
    { "id": "skill-stickiness-21", "present": true, "quotes": ["even a 1% chance a drovr skill applies means", "invoke it, paired with the cost-lowering clause (if it turns out not to fit, drop it — invoking", "costs almost nothing)."] },
    { "id": "skill-stickiness-22", "present": true, "quotes": ["**The per-turn rule**: check before any response, including before a clarifying question and", "before any read-only exploration."] },
    { "id": "skill-stickiness-23", "present": true, "quotes": ["**Instruction-priority ladder** [tier 3]: human's explicit instructions > drovr skills >", "default behaviour. This is the safety counterweight to the MUST language and is not optional."] },
    { "id": "skill-stickiness-24", "present": true, "quotes": ["a fenced `dot` flowchart (branching, per §3's placement rule): message", "received → does any skill apply (≥1%) → invoke → announce → does it have a checklist → create", "one tracked item per step → follow it → only then respond."] },
    { "id": "skill-stickiness-25", "present": true, "quotes": ["Red-flag table for the router's own failure mode** (invoking nothing at all — drovr currently", "Candidate rows, finalized by fix 5's RED pass rather than fixed", "be overkill so no skill applies.\"*"] },
    { "id": "skill-stickiness-26", "present": true, "quotes": ["Items 2–5 sit outside every `<!-- reflex:section:NAME -->` marker [tier 4 — superpowers has no", "`[reflex.sections]` may subtract advisory sections but must not be", "able to silently delete the routing core; only `[reflex] enabled = false` removes it."] },
    { "id": "skill-stickiness-27", "present": true, "quotes": ["`hooks/hooks.json` gains a `UserPromptSubmit` entry running a new, thin `hooks/user-prompt`", "script that `exec`s `drovr reflex --gate`."] },
    { "id": "skill-stickiness-28", "present": true, "quotes": ["`UserPromptSubmit` takes **no** `matcher` — the", "existing `SessionStart` entry's `startup|clear|compact` matcher is not copied across."] },
    { "id": "skill-stickiness-29", "present": true, "quotes": ["**Byte budget, not tokens** — the CLI has no tokenizer. The assertion is rendered", "`additionalContext` ≤ 600 bytes, checked in `cli/src/reflex.rs`'s test module."] },
    { "id": "skill-stickiness-30", "present": true, "quotes": ["**Suppression rule** (bounds the cumulative cost): the card is emitted only when no `drovr:*`", "skill was invoked in the previous turn."] },
    { "id": "skill-stickiness-31", "present": true, "quotes": ["**Card content:** the 1% rule, the per-turn check, the announcement string, the", "checklist-binding line, a `<SUBAGENT-STOP>` line, and a pointer to `Skill drovr:using-drovr`."] },
    { "id": "skill-stickiness-32", "present": true, "quotes": ["`reflex.rs:143-152`'s `envelope()` hardcodes `\"hookEventName\": \"SessionStart\"` —", "parameterize it to take the event name."] },
    { "id": "skill-stickiness-33", "present": true, "quotes": ["`main.rs:114-118` declares `Reflex { skill: PathBuf }` required, and `main.rs:1276-1278`", "asserts bare `drovr reflex` errors. `--gate` makes it `Option<PathBuf>` with a clap arg-group", "rule (`--gate` xor `--skill`); that test is updated accordingly."] },
    { "id": "skill-stickiness-34", "present": true, "quotes": ["Card text lives as a `const` in `reflex.rs`, not extracted from `SKILL.md` — extraction would"] },
    { "id": "skill-stickiness-35", "present": true, "quotes": ["The resulting drift risk between card and skill is mitigated by a test asserting the card's", "key phrases appear in `using-drovr/SKILL.md`."] },
    { "id": "skill-stickiness-36", "present": true, "quotes": ["Whether `UserPromptSubmit` actually fires", "for Agent-tool subagents in this harness is verified empirically as build step 1 (§0), and the", "answer is recorded in `docs/skill-evidence/per-turn-gate.md`."] },
    { "id": "skill-stickiness-37", "present": true, "quotes": ["new `[reflex] per_turn` bool in `ReflexConfig` (`cli/src/config.rs:39-64`), default", "`true` [tier 4], suppressible per-user, via a named default fn (not bare `#[serde(default)]`,", "per the existing trap at `config.rs:108-110`)."] },
    { "id": "skill-stickiness-38", "present": true, "quotes": ["the full `SessionStart` reflex stays suppressed inside", "phases (`DROVR_PHASE` set), because a phase agent runs on its injected briefing. The per-turn", "gate does **not** suppress inside phases — a phase is exactly where the discipline must hold."] },
    { "id": "skill-stickiness-39", "present": true, "quotes": ["`PreToolUse` hard-denial (e.g., blocking an `Edit` that skipped `drovr:tdd`, or a `git commit`", "with no verification evidence) is enforcement rather than persuasion, carries real", "false-positive risk, and is not part of this run."] },
    { "id": "skill-stickiness-40", "present": true, "quotes": ["When a skill or briefing gives you a numbered checklist, create **one tracked item per step**", "using whatever task tool this harness exposes — `TodoWrite`, or `TaskCreate`/`TaskUpdate` —", "before you start step 1."] },
    { "id": "skill-stickiness-41", "present": true, "quotes": ["If the harness exposes no task tool, write the checklist to", "`~/.local/share/drovr/runs/<run>/checklist.md` when inside a run, or `CHECKLIST.md` at the repo", "root otherwise, and tick items there."] },
    { "id": "skill-stickiness-42", "present": true, "quotes": ["`skills/using-drovr/SKILL.md` — the gate function's checklist branch (§6.1 item 5).", "`skills/pipeline/phase-prompts/*.md` — the `## Do` lists (`implement-task.md:14-64`, 7 steps,", "`skills/handoff/SKILL.md` and `skills/handoff/HANDOFF-template.md` — the 7-section handoff is a"] },
    { "id": "skill-stickiness-43", "present": true, "quotes": ["1. description:      trigger, not summary (fix 1)", "2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines", "4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,"] },
    { "id": "skill-stickiness-44", "present": true, "quotes": ["7. Requirements      claim → required evidence → not sufficient", "ONLY: verification-before-completion, code-review"] },
    { "id": "skill-stickiness-45", "present": true, "quotes": ["6b. Cycle flowchart  a fenced dot graph of the loop", "ONLY: tdd, systematic-debugging   (§3 placement rule)"] },
    { "id": "skill-stickiness-46", "present": true, "quotes": ["**Announcement strings** (template `Using drovr:<skill> to <purpose>.`):", "`tdd` — *\"Using drovr:tdd — writing the failing test before the implementation.\"*", "`systematic-debugging` — *\"Using drovr:systematic-debugging — reproducing before fixing.\"*"] }
  ]
}
