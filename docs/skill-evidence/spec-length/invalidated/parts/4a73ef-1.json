{ "spec_id": "4a73ef", "ledger": "skill-stickiness",
  "rows": [
    { "id": "skill-stickiness-01", "present": true, "quotes": ["Capture arm A / RED per skill (§6.3) at the pre-fix `HEAD`, before any skill file is touched.", "Hard gate: once fix 1 lands, arm A is unrecoverable without a checkout."] },
    { "id": "skill-stickiness-02", "present": true, "quotes": ["1. **Published evidence exists** → the evidence decides; the citation stays in the shipped text.\n2. **No evidence, but reachable by this run's loop** → **measure it** (§6.4).", "3. **Not reachable, or measurement inconclusive, and superpowers has a convention** → **follow\n   superpowers**, and label it a convention-follow, not evidence-backed.\n4. **No superpowers analogue** → **engineering judgement, marked `[tier 4]`.**"] },
    { "id": "skill-stickiness-03", "present": true, "quotes": ["**No fabricated measurements.**", "No number, duration, frequency, or comparative is stated as\n  measured unless drovr measured it or a citation supports it"] },
    { "id": "skill-stickiness-04", "present": true, "quotes": ["**No copied text.**", "Both projects are MIT; mechanisms are ported, sentences are drovr's own.", "Any verbatim ≥8-word shingle found in review (§8's check) gets reworded or the MIT notice and\n  credit added."] },
    { "id": "skill-stickiness-05", "present": true, "quotes": ["**Unity is adopted for the four discipline skills**, deviating from superpowers (which", "the next phase agent is you, with your context gone"] },
    { "id": "skill-stickiness-06", "present": true, "quotes": ["each bullet pairing the prohibition with the required action"] },
    { "id": "skill-stickiness-07", "present": true, "quotes": ["the reality column is an INSTRUCTION, not a rebuttal"] },
    { "id": "skill-stickiness-08", "present": true, "quotes": ["fenced `dot` blocks **only** for genuine branching or a stop-too-early loop, never for linear\n  procedure or reference material."] },
    { "id": "skill-stickiness-09", "present": true, "quotes": ["**Gate function**, a fenced `dot` flowchart", "**Loop** (fenced `dot` flowchart — a stop-too-early loop, per §1)"] },
    { "id": "skill-stickiness-10", "present": true, "quotes": ["Rendering `dot` blocks in the review UI, and pairing a phase/gate graph with the plan display,", "Filed as a `docs/known-issues.md` follow-up note, not built here."] },
    { "id": "skill-stickiness-11", "present": true, "quotes": ["teaching content stays terse and is cross-referenced, never", "binding content (the Iron Law, the matching red flag, the matching rationalization row,", "the matching checklist item) is deliberately redundant, stated 3–4 times per skill"] },
    { "id": "skill-stickiness-12", "present": true, "quotes": ["`cli/tests/skills_valid.rs:17`'s `const BODY_BUDGET`", "(currently `2200` bytes, checked over exactly these four skills at `:20-25`", "rises to **12000 bytes**."] },
    { "id": "skill-stickiness-13", "present": true, "quotes": ["`using-drovr` gets its\nown new cap and joins the checked list at **9000 bytes**"] },
    { "id": "skill-stickiness-14", "present": true, "quotes": ["`cli/tests/skills_valid.rs`'s byte check is the", "**single authoritative size check**; `wc -l`/`wc -w` are author guidance only, never asserted."] },
    { "id": "skill-stickiness-15", "present": true, "quotes": ["Above cap, split into `references/`."] },
    { "id": "skill-stickiness-16", "present": true, "quotes": ["all four discipline skills scope themselves to drovr phases in", "returns all 8 files — none of those literals survives anywhere."] },
    { "id": "skill-stickiness-17", "present": true, "quotes": ["a `description:` that\nsummarizes the workflow (current `using-drovr:3`) lets an agent skip reading the skill body,", "which treats the trigger description as the\nhighest-leverage line."] },
    { "id": "skill-stickiness-18", "present": true, "quotes": ["Use at the start of every session and before every response, including before clarifying questions and before reading any file — routes to the right drovr skill and requires invoking it whenever there is even a 1% chance one applies"] },
    { "id": "skill-stickiness-19", "present": true, "quotes": ["every drovr-phase reference in these four skills becomes a clearly-marked\n*additional* consequence, never a precondition:"] },
    { "id": "skill-stickiness-20", "present": true, "quotes": ["one entry, in the file's existing convention (`## <symptom title> —", "recording that four descriptions scoped unconditional discipline to phases", "One entry only — the remaining fixes are design\nwork, not defect repair."] },
    { "id": "skill-stickiness-21", "present": true, "quotes": ["even a 1% chance a drovr skill applies means invoke it. Paired with a cost-lowering", "clause — if it turns out not to fit, drop it; invoking costs almost nothing."] },
    { "id": "skill-stickiness-22", "present": true, "quotes": ["check before any response, including before a clarifying question and", "before any read-only exploration."] },
    { "id": "skill-stickiness-23", "present": true, "quotes": ["the human's explicit instructions > drovr skills >", "default behaviour."] },
    { "id": "skill-stickiness-24", "present": true, "quotes": ["a fenced `dot` flowchart (branching, per §1's flowchart rule): message", "received → does any skill apply (≥1%) → invoke → announce → does it have a checklist → create", "one tracked item per step → follow it → only then respond."] },
    { "id": "skill-stickiness-25", "present": true, "quotes": ["Red-flag table for the router's own failure mode** (invoking nothing at all — currently zero", "Final wording comes\n   from RED (§6), not from this list."] },
    { "id": "skill-stickiness-26", "present": true, "quotes": ["items 2–5 sit outside every `<!-- reflex:section:NAME -->` marker", "`[reflex.sections]` may subtract\nadvisory sections but must never be able to silently delete the routing core; only\n`[reflex] enabled = false` removes it."] },
    { "id": "skill-stickiness-27", "present": true, "quotes": ["`hooks/hooks.json` gains a `UserPromptSubmit` entry running a new thin `hooks/user-prompt`", "script that `exec`s `drovr reflex --gate`."] },
    { "id": "skill-stickiness-28", "present": true, "quotes": ["`UserPromptSubmit` takes **no** `matcher` — the", "existing `SessionStart` entry's `startup|clear|compact` matcher must not be copied across."] },
    { "id": "skill-stickiness-29", "present": true, "quotes": ["**Byte budget**: rendered `additionalContext` ≤ 600 bytes per injection", "Length is bytes, not tokens (the CLI has no\n  tokenizer); asserted in `cli/src/reflex.rs`'s test module."] },
    { "id": "skill-stickiness-30", "present": true, "quotes": ["the card is emitted only when no `drovr:*`", "skill was invoked in the previous turn."] },
    { "id": "skill-stickiness-31", "present": true, "quotes": ["the 1% rule, the per-turn check, the announcement string, the", "checklist-binding line, a `<SUBAGENT-STOP>` line, and a pointer to `Skill drovr:using-drovr`."] },
    { "id": "skill-stickiness-32", "present": true, "quotes": ["`reflex.rs:143-152`'s `envelope()` hardcodes `\"hookEventName\": \"SessionStart\"` — must be", "parameterized to take the event name."] },
    { "id": "skill-stickiness-33", "present": true, "quotes": ["`main.rs:114-118` declares `Reflex { skill: PathBuf }` as required", "Adding `--gate` requires `Option<PathBuf>`, a clap", "arg-group rule (`--gate` xor `--skill`), and an update to that test."] },
    { "id": "skill-stickiness-34", "present": true, "quotes": ["a `const` in `reflex.rs`, not extraction from `SKILL.md`"] },
    { "id": "skill-stickiness-35", "present": true, "quotes": ["Drift risk is mitigated by a test asserting the card's key phrases appear in", "`using-drovr/SKILL.md`."] },
    { "id": "skill-stickiness-36", "present": true, "quotes": ["§0 step 1 verifies empirically whether the hook fires for Agent-tool", "subagents in this harness, recording the answer in `docs/skill-evidence/per-turn-gate.md`."] },
    { "id": "skill-stickiness-37", "present": true, "quotes": ["new `[reflex] per_turn` bool in `ReflexConfig` (`cli/src/config.rs:39-64`), default", "`true` `[tier 4]`, suppressible per-user, via a named default fn (not bare", "`#[serde(default)]` — the trap documented at `config.rs:108-110`)."] },
    { "id": "skill-stickiness-38", "present": true, "quotes": ["The full `SessionStart` reflex stays suppressed inside", "gate does **not** suppress in phases — a phase is exactly where the discipline must hold."] },
    { "id": "skill-stickiness-39", "present": true, "quotes": ["`PreToolUse` hard-denial (e.g. blocking an `Edit` that skipped `drovr:tdd`, or a `git commit`", "false-positive risk. Not this run."] },
    { "id": "skill-stickiness-40", "present": true, "quotes": ["create **one tracked item per step**", "using whatever task tool this harness exposes — `TodoWrite`, or `TaskCreate`/`TaskUpdate` —", "before you start step 1."] },
    { "id": "skill-stickiness-41", "present": true, "quotes": ["write the checklist to", "`~/.local/share/drovr/runs/<run>/checklist.md` when inside a run, or `CHECKLIST.md` at the repo", "root otherwise, and tick items there."] },
    { "id": "skill-stickiness-42", "present": true, "quotes": ["1. `skills/using-drovr/SKILL.md` — the gate function's checklist branch (§3.1 item 5).", "3. `skills/pipeline/phase-prompts/*.md` — the `## Do` lists (`implement-task.md:14-64`, 7 steps,", "4. `skills/handoff/SKILL.md` and `skills/handoff/HANDOFF-template.md` — the 7-section handoff is"] },
    { "id": "skill-stickiness-43", "present": true, "quotes": ["1. description:      trigger, not summary (fix 1)\n2. Overview          core principle + spirit-vs-letter line + the WHY, ≤6 lines\n3. Unity line        \"the next phase agent is you, with your context gone\"", "4. The Iron Law      one fenced all-caps line + \"No exceptions:\" bullets closing named loopholes,", "8. Red flags — STOP  inner-monologue fragments, closed by a catch-all bullet\n9. Rationalizations  thought → reality; the reality column is an INSTRUCTION, not a rebuttal\n10. Worked example   one ✅/❌ pair of actual utterances. One good example, not five\n11. Cross-refs       bare skill names with REQUIRED markers; never @-links (they force-load)"] },
    { "id": "skill-stickiness-44", "present": true, "quotes": ["7. Requirements      claim → required evidence → not sufficient\n                     ONLY: verification-before-completion, code-review"] },
    { "id": "skill-stickiness-45", "present": true, "quotes": ["6b. Cycle flowchart  a fenced dot graph of the loop\n                     ONLY: tdd, systematic-debugging   (§1 flowchart rule)"] },
    { "id": "skill-stickiness-46", "present": true, "quotes": ["**Announcement sentences** — template `Using drovr:<skill> to <purpose>.`:", "`tdd` — *\"Using drovr:tdd — writing the failing test before the implementation.\"*", "`code-review` — *\"Using drovr:code-review — dispatching read-only reviewers before calling this"] }
  ]
}
