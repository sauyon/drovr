**Write the spec** to `~/.local/share/drovr/runs/<run>/spec.md`: what was decided, the
   interfaces and contracts it binds, what is out of scope. Write it for a reviewer who
   was not in the interview — a decision that needs a reason gets one, in a line. No open
   questions: post the ask, get the answer, record it as a decision.
